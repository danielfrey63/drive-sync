# rclone filter rules for the "D:\Meine Ablage" <-> gdrive: bisync.
# First matching rule wins; no leading slash = matches at any depth.
# NOTE: changing this file requires a one-time "bisync --resync" afterwards.
# Decisions (2026-08-07): the corpus may host git working trees; everything
# reproducible (.git via remotes, package caches, build outputs) is excluded.
# NOT excluded despite being dev dirs - name collisions with real content:
# bin (1135x), obj (472x), vendor (36x). .vscode stays synced (tiny, useful).

# Version control / IDE churn (reproducible via git remotes)
- .git/**
- .idea/**

# Legacy version control metadata (measured 2026-08-09: CVS 54'882 dirs /
# 324k files, .svn 5'225 dirs / 65k files - pure churn, spot-checked as
# genuine VCS metadata, no name collisions with real content)
- .svn/**
- CVS/**

# Eclipse workspace state and generated API docs (measured 2026-08-09:
# .metadata 76k files / 4.2 GiB, javadoc+apidocs 37k files; spot-checked
# as workspace caches and javadoc output only)
- .metadata/**
- javadoc/**
- apidocs/**

# Secrets: environment files with live keys never go to the cloud
- .env
- .env.*
- *.env

# Package managers / language environments
- node_modules/**
- __pycache__/**
- .venv/**
- venv/**
- .tox/**
- site-packages/**
- .pytest_cache/**
- .mypy_cache/**
- .ruff_cache/**
- .turbo/**

# Build outputs
- build/**
- target/**
- dist/**
- out/**
- .next/**
- .cache/**
- .gradle/**

# DriveFS pointer files for Google-native docs (the docs themselves stay
# cloud-only via --drive-skip-gdocs; pointers stay local for the gdrive skill)
- *.gdoc
- *.gsheet
- *.gslides
- *.gdraw
- *.gform
- *.gtable
- *.glink
- *.gmaps

# Dead references in a foreign shared folder: the owner deleted the files,
# the listing still shows them, every copy attempt gets a hard 404 and
# aborts the resync (verified twice, 2026-08-07)
- /Agile Lean Teal/Teal/Intro Presentations \[shared w／ providers\]/Slide Fonts/**

# DriveFS remnants and Windows system junk
- .tmp.driveupload/**
- $RECYCLE.BIN/**
- System Volume Information/**
- desktop.ini
- Thumbs.db
- ~$*
